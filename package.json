{
  "name": "ingredify-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "frontend"
  ],
  "scripts": {
    "web": "npm run dev --workspace=frontend",
    "backend": "cd backend && uvicorn app.main:app --reload --port 8000",
    "build:web": "npm run build --workspace=frontend"
  },
  "description": "Ingredify monorepo"
}